const Fs = require('fs');
const Pdf = require('./lib/pdf-parse.js');

module.exports = Pdf;

// pdf-parse has a debug/test block that may run when imported under
// certain ESM/CJS interop conditions. That block attempts to read
// a local test PDF ('./test/data/05-versions-space.pdf') which often
// doesn't exist in consumer projects and causes an ENOENT error.
// Disable the debug execution here to avoid crashing on import.
let isDebugMode = false; // force debug off for embedded usage
